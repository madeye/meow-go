<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Debug-only overlay: allow cleartext HTTP so the e2e fixture
         served from http://10.0.2.2:8080/nodelist.txt (python http.server)
         is reachable. Release builds keep the platform default (blocked). -->
    <application android:usesCleartextTraffic="true" />
</manifest>
